"""Internal sub-modules backing :mod:`demodsl.effects.timeline_compositor`.

These are pure leaf helpers extracted from the historical monolithic
``timeline_compositor`` module. They are imported back into that module
to preserve the public surface used by tests and other call sites.
"""
