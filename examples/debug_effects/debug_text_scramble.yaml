# Auto-generated — single effect debug: text_scramble
metadata:
  title: "Debug — text_scramble"
  version: "2.0.0"

scenarios:
  - name: "text_scramble"
    url: "https://fran-cois.github.io/demodsl/"
    browser: "chrome"
    viewport: { width: 1280, height: 720 }
    pre_steps:
      - action: "navigate"
        url: "https://fran-cois.github.io/demodsl/"
        wait: 2.0
    steps:
      - action: "scroll"
        direction: "down"
        pixels: 50
        narration: "Effect: text_scramble"
        wait: 3.5
        effects:
          - type: "text_scramble"
            speed: 50

pipeline:
  - optimize:
      format: mp4
      codec: h264
      quality: high

output:
  filename: "debug_text_scramble.mp4"
  directory: "output/debug_effects"
