# Auto-generated — single effect debug: glow
metadata:
  title: "Debug — glow"
  version: "2.0.0"

scenarios:
  - name: "glow"
    url: "https://fran-cois.github.io/demodsl/"
    browser: "chrome"
    viewport: { width: 1280, height: 720 }
    pre_steps:
      - action: "navigate"
        url: "https://fran-cois.github.io/demodsl/"
        wait: 2.0
    steps:
      - action: "scroll"
        direction: "down"
        pixels: 50
        narration: "Effect: glow"
        wait: 3.5
        effects:
          - type: "glow"
            duration: 2.5
            color: "#6366f1"

pipeline:
  - optimize:
      format: mp4
      codec: h264
      quality: high

output:
  filename: "debug_glow.mp4"
  directory: "output/debug_effects"
