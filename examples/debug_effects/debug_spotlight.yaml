# Auto-generated — single effect debug: spotlight
metadata:
  title: "Debug — spotlight"
  version: "2.0.0"

scenarios:
  - name: "spotlight"
    url: "https://fran-cois.github.io/demodsl/"
    browser: "chrome"
    viewport: { width: 1280, height: 720 }
    pre_steps:
      - action: "navigate"
        url: "https://fran-cois.github.io/demodsl/"
        wait: 2.0
    steps:
      - action: "scroll"
        direction: "down"
        pixels: 50
        narration: "Effect: spotlight"
        wait: 3.5
        effects:
          - type: "spotlight"
            duration: 2.5
            intensity: 0.8

pipeline:
  - optimize:
      format: mp4
      codec: h264
      quality: high

output:
  filename: "debug_spotlight.mp4"
  directory: "output/debug_effects"
