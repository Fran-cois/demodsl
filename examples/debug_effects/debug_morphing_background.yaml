# Auto-generated — single effect debug: morphing_background
metadata:
  title: "Debug — morphing_background"
  version: "2.0.0"

scenarios:
  - name: "morphing_background"
    url: "https://fran-cois.github.io/demodsl/"
    browser: "chrome"
    viewport: { width: 1280, height: 720 }
    pre_steps:
      - action: "navigate"
        url: "https://fran-cois.github.io/demodsl/"
        wait: 2.0
    steps:
      - action: "scroll"
        direction: "down"
        pixels: 50
        narration: "Effect: morphing_background"
        wait: 3.5
        effects:
          - type: "morphing_background"
            colors: ["#667eea", "#764ba2", "#f093fb"]

pipeline:
  - optimize:
      format: mp4
      codec: h264
      quality: high

output:
  filename: "debug_morphing_background.mp4"
  directory: "output/debug_effects"
