# Auto-generated — single effect debug: star_burst
metadata:
  title: "Debug — star_burst"
  version: "2.0.0"

scenarios:
  - name: "star_burst"
    url: "https://fran-cois.github.io/demodsl/"
    browser: "chrome"
    viewport: { width: 1280, height: 720 }
    pre_steps:
      - action: "navigate"
        url: "https://fran-cois.github.io/demodsl/"
        wait: 2.0
    steps:
      - action: "scroll"
        direction: "down"
        pixels: 50
        narration: "Effect: star_burst"
        wait: 3.0
        effects:
          - type: "star_burst"
            duration: 2.0

pipeline:
  - optimize:
      format: mp4
      codec: h264
      quality: high

output:
  filename: "debug_star_burst.mp4"
  directory: "output/debug_effects"
